<div class="card mb-4">
    <div class="card-body">
        <h5 class="cart-title">
            <a href="{{path('property.show' , {'id': property.id, 'slug': property.slug} )}}""> {{property.title}} </a>
        </h5>
        <p class="card-text"> {{ property.city }} {{ property.zipcode }}</p>
        <div calss="text-primary" style="font-size:2rem">
            {{ property.price }}
        </div>
    </div>
</div>